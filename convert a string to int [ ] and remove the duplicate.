import java.util.Scanner;
class achu {
    public static void main(String[] args) {
        String a = "131,11,48";
        String b[] = a.split(",");
        String p = "";
        for (String h : b) {
            p += h; 
        }
        
        int len = p.length();
        int[] m = new int[len];
        int count = 0;
        for (int i = 0; i < len; i++) {
           m[i]=p.charAt(i)-'0';
        }
        
        
        for (int i = 0; i < len; i++) {
            boolean found = false;
            for (int j = 0; j < i; j++) {
                if (m[i] == m[j]) {
                    found = true;
                    break;
                }
            }
            if (!found) {
                System.out.print(m[i] + " ");
            }
        }
    }
}
